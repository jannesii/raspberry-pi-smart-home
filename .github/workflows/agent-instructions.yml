name: Agent instruction budgets

on: [push, pull_request]

permissions:
  contents: read

jobs:
  instruction-budget:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python3 server/scripts/check_agent_instructions.py
